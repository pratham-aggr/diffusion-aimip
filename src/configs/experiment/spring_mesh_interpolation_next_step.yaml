# @package _global_

# to execute this experiment run:
# python run.py experiment=spring_mesh_interpolation_next_step

defaults:
  - spring_mesh.yaml
  - override /module: interpolation_next_step.yaml
  - _self_

name: "SpringMesh-IpolNext${datamodule.horizon}h"

module:
  enable_inference_dropout: True

#trainer:
#  max_epochs: 400

logger:
  wandb:
    tags: ["spring-mesh", "interpolation"]
