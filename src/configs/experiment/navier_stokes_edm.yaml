# @package _global_

# to execute this experiment run:
# python run.py experiment=navier_stokes_edm

defaults:
  - navier_stokes.yaml
  - override /module: forecasting_multi_horizon_at_once.yaml
  - override /diffusion: edm.yaml
  - _self_

name: "NS-MH${datamodule.horizon}-EDM"

model:
  with_time_emb: True

logger:
  wandb:
    tags: ["navier-stokes", 'EDM']