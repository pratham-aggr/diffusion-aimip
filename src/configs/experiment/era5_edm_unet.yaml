# @package _global_

# to execute this experiment run:
# python run.py experiment=era5_edm_unet

defaults: 
  - era5_genie_ar_global.yaml
  # - override /model: adm.yaml
  - override /diffusion: edm.yaml
  - _self_

name: "GENIE-${datamodule.hourly_resolution}h-${datamodule.horizon}AR-EDM"

model:
  with_time_emb: True

diffusion:
  num_steps: 12

datamodule:
  spatial_crop_outputs: null
  output_mask_area: null