# @package _global_

# to execute this experiment run:
# python run.py experiment=navier_stokes_pde_refiner

defaults:
  - navier_stokes.yaml
  - override /module: pde_refiner.yaml
  - override /diffusion: pde_refiner.yaml
  - _self_

name: "NS-MH${datamodule.horizon}-PDERef-${diffusion.num_refinement_steps}k-${diffusion.min_noise_std}minstd"
datamodule:
  horizon: 1